{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai2m2ia.github.io/api/schemas/catalog.schema.json",
  "title": "AI2M2IA Books Catalog (Hub-and-Spoke)",
  "type": "object",
  "required": ["schemaVersion", "generatedAt", "apiBaseUrl", "apiPrefix", "books"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 2 },
    "generatedAt": { "type": "string", "format": "date-time" },
    "apiBaseUrl": { "type": "string", "format": "uri" },
    "apiPrefix": { "type": "string", "pattern": "^/" },
    "books": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "title", "spokeUrl", "languages", "author"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "title": { "type": "string", "minLength": 1 },
          "format": { "enum": ["PROSE", "LIGHT_NOVEL", "MANGA", "MANHWA"] },
          "spokeUrl": { "type": "string", "format": "uri", "pattern": "^https://" },
          "languages": {
            "type": "array",
            "items": { "type": "string", "minLength": 2 },
            "minItems": 1
          },
          "author": { "type": "string", "minLength": 1 },
          "coverUrl": { "type": ["string", "null"], "format": "uri" },
          "description": { "type": ["string", "null"] }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
